# Tasks

## Task 1: Update HTML structure — replace grid with carousel markup and correct certifications

- [x] 1.1 Replace the `certifications__grid` div and its 4 incorrect badges with the new carousel HTML structure: `certifications__carousel` wrapper with `role="region"`, `aria-roledescription="carousel"`, and `aria-label="Certifications"`
- [x] 1.2 Add the `certifications__viewport` and `certifications__track` (with `aria-live="polite"`) containing 6 `certifications__slide` elements with the correct certification data: "Microsoft Certified: Microsoft Azure Architect Technologies" (Feb 2021), "Microsoft Certified: Azure Administrator Associate" (Jan 2020), "Microsoft Certified: Azure Fundamentals" (Aug 2022), "Microsoft OpenHack: DevOps" (May 2022), "Microsoft OpenHack: Serverless" (Nov 2022), "Scrum.org: Professional Scrum Master I (PSM I)" (Aug 2024)
- [x] 1.3 Add previous and next `certifications__arrow` buttons with SVG chevron icons, `aria-label` attributes ("Previous certification" / "Next certification"), and the prev arrow starting with `aria-disabled="true"`
- [x] 1.4 Add `certifications__pagination` container with `role="tablist"` and `aria-label="Certification slides"` (dots will be generated by JS)

## Task 2: Add carousel CSS styles to sections.css

- [x] 2.1 Replace the existing `.certifications__grid` styles with `.certifications__carousel` layout styles: `position: relative`, flex column for pagination below track
- [x] 2.2 Add `.certifications__viewport` styles: `overflow: hidden`, `width: 100%`
- [x] 2.3 Add `.certifications__track` styles: `display: flex`, `transition: transform var(--duration-normal) var(--ease-out-quart)`, `will-change: transform`
- [x] 2.4 Add `.certifications__slide` styles: `flex: 0 0 calc(100% / var(--slides-per-view))`, `box-sizing: border-box`, padding for gap between slides
- [x] 2.5 Add `.certifications__arrow` styles: positioned on left/right of viewport, `min-width: 44px`, `min-height: 44px`, centered flex, border-radius, hover/focus states, and `--disabled` modifier with reduced opacity and `pointer-events: none`
- [x] 2.6 Add `.certifications__pagination` and `.certifications__dot` styles: centered flex row, dot size ~10px circles, `--active` modifier using `var(--color-accent)`, hover/focus states, transition on background-color

## Task 3: Add responsive breakpoints for --slides-per-view

- [x] 3.1 Add `--slides-per-view: 3` on `.certifications__carousel` as the default (desktop >1024px)
- [x] 3.2 Add media query in responsive.css: at `max-width: 1024px`, set `--slides-per-view: 2` on `.certifications__carousel`
- [x] 3.3 Add media query in responsive.css: at `max-width: 768px`, set `--slides-per-view: 1` on `.certifications__carousel`
- [x] 3.4 Add responsive adjustments for arrow positioning and pagination spacing at tablet and mobile breakpoints

## Task 4: Implement carousel.js module with core navigation

- [x] 4.1 Create `js/carousel.js` with `initCarousel` export that queries `.certifications__carousel`, reads `--slides-per-view` from computed styles, computes `maxIndex`, and stores state in a closure
- [x] 4.2 Implement `goToSlide(index)` that clamps index to `[0, maxIndex]`, sets `currentIndex`, calls `updateTrackPosition()`, `updateArrows()`, and `updateDots()`
- [x] 4.3 Implement `updateTrackPosition(animate)` that computes `translateX(-(currentIndex * slideWidth))` and applies it to the track element, with `isTransitioning` guard and `transitionend` / timeout fallback
- [x] 4.4 Implement `updateArrows()` that sets `aria-disabled` and toggles `certifications__arrow--disabled` class based on `currentIndex === 0` (prev) and `currentIndex === maxIndex` (next)
- [x] 4.5 Implement `updateDots()` that generates dot buttons dynamically (count = `maxIndex + 1`), toggles `certifications__dot--active` and `aria-selected` on the dot matching `currentIndex`
- [x] 4.6 Add click event listeners on prev/next arrows calling `goToSlide(currentIndex - 1)` and `goToSlide(currentIndex + 1)`, with `isTransitioning` guard to ignore clicks during animation

## Task 5: Add touch/swipe support to carousel

- [x] 5.1 Add `touchstart` listener (passive) on the track that records `touchStartX`, `touchStartY`, sets `isDragging = true`
- [x] 5.2 Add `touchmove` listener on the track that computes `dragDelta`, applies live drag offset `translateX(baseOffset + dragDelta)` without transition, and calls `preventDefault()` only when horizontal swipe threshold is met
- [x] 5.3 Add `touchend` listener that evaluates swipe: if `|deltaX| >= 30` and `|deltaX| > |deltaY|`, call `goToSlide` in the appropriate direction; otherwise snap back to current position

## Task 6: Add keyboard navigation and resize handling

- [x] 6.1 Add `keydown` listener on the carousel element for `ArrowLeft` (prev) and `ArrowRight` (next) navigation
- [x] 6.2 Add `resize` event listener (debounced) that re-reads `--slides-per-view`, recomputes `maxIndex`, clamps `currentIndex`, regenerates dots, and updates track position without animation

## Task 7: Integrate carousel module into app.js

- [x] 7.1 Add `import { initCarousel } from './carousel.js'` to `js/app.js`
- [x] 7.2 Add `initCarousel()` call inside the `init()` function alongside existing module initializations

## Task 8: Write property-based tests

- [x] 8.1 Create `js/carousel.test.js` with test setup: jsdom environment, import `@fast-check/vitest` and `fast-check`
- [x] 8.2 Write property test for index clamping (Property 1): for any totalSlides, slidesPerView, and requested index, goToSlide produces clamp(index, 0, max(0, totalSlides - slidesPerView))
- [x] 8.3 Write property test for track offset computation (Property 2): for any currentIndex and slideWidth, translateX equals -(currentIndex * slideWidth)
- [x] 8.4 Write property test for pagination dot count (Property 3): for any totalSlides and slidesPerView, dot count equals max(0, totalSlides - slidesPerView) + 1
- [x] 8.5 Write property test for active dot position (Property 4): for any valid currentIndex, exactly one dot has active class at the correct index
- [x] 8.6 Write property test for swipe gesture classification (Property 5): for any (deltaX, deltaY), swipe is detected iff |deltaX| >= 30 and |deltaX| > |deltaY|, with correct direction
- [x] 8.7 Write property test for drag offset (Property 6): for any currentIndex, slideWidth, and dragDelta, track translateX equals -(currentIndex * slideWidth) + dragDelta

## Task 9: Write unit tests for DOM structure and edge cases

- [x] 9.1 Write unit tests verifying 6 correct certification badges with expected names and dates (Req 1.1, 1.2, 1.3)
- [x] 9.2 Write unit tests for ARIA attributes: carousel role, arrow labels, track aria-live, disabled state at boundaries (Req 7.1–7.5)
- [x] 9.3 Write unit tests for edge cases: clicks ignored during transition (Req 8.3), no-op when carousel element missing, reduced motion handling
