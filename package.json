{
  "scripts": {
    "dev": "serve .",
    "build": "node build.mjs",
    "preview": "serve dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "serve": "^14.2.6"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.4.0",
    "esbuild": "^0.28.0",
    "fast-check": "^4.7.0",
    "gh-pages": "^6.3.0",
    "jsdom": "^29.1.0",
    "vitest": "^4.1.5"
  }
}
